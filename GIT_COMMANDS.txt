# ======================================================================
# GIT — Toutes les commandes du projet « Réservation de salles » (ODC-P8)
# Chaque étape de l'exercice = une branche feature/XX + un tag vx.y.z.
# ======================================================================
# Rejouer tout le script depuis un dossier vide :
#   mkdir reservation-salles && cd reservation-salles
#   cp -r ../chemin/vers/le-projet-complet/* .
# puis coller les blocs dans l'ordre.
# ======================================================================

# ---------------------------------
# ÉTAPE 0 — Initialisation du dépôt (branche main, tag v0.0.0)
# ---------------------------------
git init -b main
git config user.name "mhd"
git config user.email "123mouhametba123@gmail.com"
git add .gitignore README.md CHANGELOG.md
git commit -m "chore: initialisation du dépôt, README et CHANGELOG"
git tag -a v0.0.0 -m "Étape 0 : initialisation du dépôt"

# ---------------------------------
# ÉTAPE 1 — Projet Composer (branche feature/01-composer, tag v0.1.0)
# ---------------------------------
git checkout -b feature/01-composer
git add composer.json
git commit -m "init: créer le projet PHP (composer.json, autoload PSR-4)"
git add composer.lock
git commit -m "chore: installer les dépendances (composer.lock)"
git tag -a v0.1.0 -m "Étape 1 : projet Composer"

# ---------------------------------
# ÉTAPE 2 — Eloquent (branche feature/02-eloquent, tag v0.2.0)
# ---------------------------------
git checkout -b feature/02-eloquent
git add .env.example config/database.php database/migrations/001_creer_table_salles.php database/migrations/002_creer_table_reservations.php database/migrer.php
git commit -m "feat: configurer Eloquent (Capsule) et créer les tables"
git tag -a v0.2.0 -m "Étape 2 : connexion Eloquent et schéma des tables"

# ---------------------------------
# ÉTAPE 3 — Modèles (branche feature/03-modeles, tag v0.3.0)
# ---------------------------------
git checkout -b feature/03-modeles
git add src/Model/
git commit -m "feat: modèles Salle et Reservation (relations et conversions)"
git tag -a v0.3.0 -m "Étape 3 : modèles Eloquent"

# ---------------------------------
# ÉTAPE 4 — Données initiales (branche feature/04-donnees-initiales, tag v0.4.0)
# ---------------------------------
git checkout -b feature/04-donnees-initiales
git add database/seed.php
git commit -m "feat: seeder idempotent des salles initiales"
git tag -a v0.4.0 -m "Étape 4 : données initiales (5 salles)"

# ---------------------------------
# ÉTAPE 5 — Validation (branche feature/05-validation, tag v0.5.0)
# ---------------------------------
git checkout -b feature/05-validation
git add src/Validator/
git commit -m "feat: validation des formulaires (ValidatorInterface, ValidationResult, validateurs)"
git tag -a v0.5.0 -m "Étape 5 : validation (respect/validation)"

# ---------------------------------
# ÉTAPE 6 — DTO (branche feature/06-dto, tag v0.6.0)
# ---------------------------------
git checkout -b feature/06-dto
git add src/DTO/
git commit -m "feat: DTO typés CreerSalleDTO et CreerReservationDTO"
git tag -a v0.6.0 -m "Étape 6 : DTO (Data Transfer Objects)"

# ---------------------------------
# ÉTAPE 7 — Repositories (branche feature/07-repositories, tag v0.7.0)
# ---------------------------------
git checkout -b feature/07-repositories
git add src/Repository/
git commit -m "feat: repositories (interfaces et implémentations Eloquent)"
git tag -a v0.7.0 -m "Étape 7 : repositories"

# ---------------------------------
# ÉTAPE 8 — Services (branche feature/08-services, tag v0.8.0)
# ---------------------------------
git checkout -b feature/08-services
git add src/Service/ src/Exception/
git commit -m "feat: services métier (création, annulation) et exceptions"
git tag -a v0.8.0 -m "Étape 8 : services métier"

# ---------------------------------
# ÉTAPE 9 — Interface web (branche feature/09-interface-web, tag v0.9.0)
# ---------------------------------
git checkout -b feature/09-interface-web
git add src/Controller/ src/View/ templates/
git commit -m "feat: contrôleurs, moteur de vue et gabarits"
git tag -a v0.9.0 -m "Étape 9 : interface web (MVC)"

# ---------------------------------
# ÉTAPE 10 — Routeur (branche feature/10-router, tag v0.10.0)
# ---------------------------------
git checkout -b feature/10-router
git add routes/ src/Application.php
git commit -m "feat: routes du site et dispatcher FastRoute dans le front controller"
git tag -a v0.10.0 -m "Étape 10 : routeur FastRoute"

# ---------------------------------
# ÉTAPE 11 — Conteneur (branche feature/11-container, tag v0.11.0)
# ---------------------------------
git checkout -b feature/11-container
git add config/container.php public/index.php
git commit -m "feat: conteneur d'injection PHP-DI (autowiring, définitions, factories)"
git tag -a v0.11.0 -m "Étape 11 : conteneur d'injection"

# ---------------------------------
# ÉTAPE 12 — Tests (branche feature/12-tests, tag v0.12.0)
# ---------------------------------
git checkout -b feature/12-tests
git add phpunit.xml tests/
git commit -m "feat: tests unitaires et d'intégration (PHPUnit)"
git tag -a v0.12.0 -m "Étape 12 : tests (PHPUnit)"

# ---------------------------------
# ÉTAPE 13 — Release (branche release/1.0.0, tag v1.0.0)
# ---------------------------------
git checkout -b release/1.0.0
git add public/assets/style.css
git commit -m "style: mise en forme CSS du site"
git add Dockerfile docker-compose.yml .dockerignore docker/
git commit -m "chore: dockeriser le projet (Nginx + PHP-FPM, docker-compose, entrypoint)"
git add README.md CHANGELOG.md ARCHITECTURE.md docs/ GIT_COMMANDS.txt
git commit -m "docs: documentation complète (README, CHANGELOG, ARCHITECTURE, diagramme de classes)"
git tag -a v1.0.0 -m "Étape 13 : release 1.0.0"

# ---------------------------------
# Réintégration finale dans main (optionnel)
# ---------------------------------
git checkout main
git merge --no-ff release/1.0.0 -m "merge: intégration de la release 1.0.0 dans main"
git log --oneline --graph --all --decorate   # vérification
git tag -n                                       # liste des tags